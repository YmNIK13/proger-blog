## Memory Bank

- Read all Markdown files in `docs/memory-bank/` at the start of every task.
- Use `docs/memory-bank/` as the primary short-form project context layer.
- Use `docs/technical/architecture.md` for deep technical context about the `proger-blog` theme.
- Treat `web/app/themes/proger-blog` as the main custom artifact in the repository.
