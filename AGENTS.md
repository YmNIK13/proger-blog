# AGENTS.md

## Startup

- Read all Markdown files in `docs/memory-bank/` at the start of every task.
- Use `docs/memory-bank/` as the primary source of general project context.
- Use `docs/technical/architecture.md` for deep theme-level technical context.

## Source Of Truth

- For current runtime behavior, prioritize code and in-repo documentation.
- Treat `web/app/themes/proger-blog` as the main custom artifact in the repository.
- Treat `openspec/` as planning material unless the task explicitly asks to work on it.

## Working Rules

- Start WordPress and theme work by inspecting the actual theme files first.
- Keep repository-level documentation focused on the overall blog project.
- Keep theme-level documentation focused on `proger-blog` as an extensible artifact.
- Do not change working code during documentation-only tasks unless the user explicitly asks for code changes.
